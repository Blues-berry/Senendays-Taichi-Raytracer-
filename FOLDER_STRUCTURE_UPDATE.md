# 文件夹结构调整

## 📁 **新的目录结构**

所有实验和基准测试数据现在统一保存在 `results/` 文件夹内：

```
results/
├── benchmark_results_20251224_111954/
│   ├── benchmark_results.csv    # 基准测试数据
│   └── result_*.png            # 各模式结果图
├── experiment_20251224_112030/
│   ├── experiment_log.csv     # 实验日志
│   ├── *.png                   # 截图文件
│   └── output_*.png           # 最终输出
└── ...
```

## 🎯 **修改内容**

### Benchmark.py
- 创建 `results/` 目录
- 基准测试文件夹改为：`results/benchmark_results_YYYYMMDD_HHMMSS/`

### Main.py  
- 创建 `results/` 目录
- 实验文件夹改为：`results/experiment_YYYYMMDD_HHMMSS/`

## ✅ **优势**

1. **统一管理**: 所有数据集中在一个 `results/` 文件夹
2. **清晰分类**: 基准测试和实验分别有独立的时间戳文件夹
3. **便于备份**: 只需要备份 `results/` 文件夹即可
4. **避免混乱**: 不再在项目根目录生成多个数据文件夹

## 🔧 **使用方法**

运行基准测试：
```bash
python benchmark.py
# 生成: results/benchmark_results_20251224_112030/
```

运行主程序：
```bash
python main.py
# 生成: results/experiment_20251224_112045/
```

现在所有的数据都会整齐地保存在 `results/` 文件夹内！