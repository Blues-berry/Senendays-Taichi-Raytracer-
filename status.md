第一阶段：解决动态场景的“拖影”与“闪烁”目前的 Hybrid 算法在物体高速移动时，网格更新可能存在时滞，导致画面出现残影或闪烁。指令 1：实现基于运动感知的动态采样（Motion-Aware Sampling）“请在 camera.py 中增强自适应逻辑。维护一个 velocity_field，记录场景中物体的运动矢量。如果某个网格点对应的区域内有物体快速移动，自动根据速度按比例提升 samples_per_probe（采样率）。同时，在物体移动的方向上提前进行‘预采样（Pre-sampling）’。这将在论文中论证你的算法具有极佳的时域一致性（Temporal Consistency）。”第二阶段：量化收敛性（支撑学术结论）论文需要证明你的“自适应”比“固定网格”到底快多少。指令 2：统计收敛速度指标（Speed to Convergence Metrics）“请修改 benchmark.py。当物体停止移动后，记录 MSE 降至稳定阈值（例如 Path Tracing 参考值的 1.05 倍）所需的帧数。比较三种情况：1. 固定低概率更新；2. 固定高概率更新；3. 你的自适应更新逻辑。产出一张横轴为‘算力消耗’，纵轴为‘收敛时间’的散点图。这将作为你论文中‘效率提升’的核心数据支持。”第三阶段：高难度场景的“压力测试”目前的球体场景太简单。你需要证明算法在复杂遮挡下的表现。指令 3：构造“窄缝漏光”极端场景（The Stress Test）“请在 main.py 的场景设置中增加一个‘两室一门’场景：两个相连的康奈尔盒，光源只在 A 房间，B 房间仅通过一道窄缝接受间接光。测试并记录：深度遮挡检测是否能彻底阻止光线‘穿墙’直接照亮 B 房间。采样是否能准确捕捉到经过门缝射入的微弱间接光。捕捉对比图并在论文中展示算法在处理复杂几何拓扑时的优越性。”第四阶段：可视化增强（论文配图专用）指令 4：生成误差热力图对比组（Error Heatmap Sequence）“请编写一个脚本，针对物体移动后的第 1、5、20、100 帧，分别生成 Hybrid 模式与 PT 模式的差值热力图（Error Heatmap）。将这些热力图排列成序列图。这在论文中能直观展示：你的算法是如何在短时间内快速‘抹平’高误差区域的。”第五阶段：显存与能效比分析指令 5：统计不同网格密度的性能开销（Trade-off Analysis）“请 Agent 统计在 $16^3, 32^3, 64^3$ 不同网格分辨率下的 GPU 显存占用（MB）和每帧更新耗时（ms）。并计算‘单位计算量下的误差下降率’。这个数据将支撑论文中关于‘算法轻量化（Lightweight）’的论点，证明其适合在移动端或低功耗 GPU 上运行。”总结建议：接下来的工作重点不再是写新功能，而是**“收集能说服审稿人的硬核数据”**。建议你先执行 指令 2 (收敛速度统计) 和 指令 4 (热力图序列)。如果你准备开始写论文初稿，我还可以为你提供 Methodology（方法论） 部分的数学公式推导建议。你需要吗？